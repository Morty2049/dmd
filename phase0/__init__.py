"""Phase 0 local prototype — THROWAWAY.

Every module in this package is marked ``# PHASE0:`` and is scheduled
for deletion when Phase 2 lands. The protocol in ``protocol/`` is the
only thing that survives the transition. See ADR-0005.
"""
